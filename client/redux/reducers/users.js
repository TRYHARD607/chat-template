const initialState = {
  name: 'TRYHARD',
  img: 'https://i.imgur.com/qACoKgY.jpg',
  tag: '#tryhard1337'
}

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
